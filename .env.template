# Supto.dev - Backend Environment Variables

# MongoDB Connection String
# For local: mongodb://localhost:27017/supto-dev
# For production (Atlas): mongodb+srv://<user>:<password>@cluster.mongodb.net/supto-dev
MONGODB_URI=your_mongodb_uri

# JWT Secret (Used for internal session signing if needed)
JWT_SECRET=your_random_secure_string

# Google OAuth Credentials (for Passport.js)
# Create these at: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Stripe Keys
# Create these at: https://dashboard.stripe.com/
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# URLs
# The URL of your deployed frontend (Next.js app)
CLIENT_URL=http://localhost:3000
# The backend URL itself (used for OAuth callbacks)
SERVER_URL=http://localhost:5000

# Firebase Admin (Choose one)
# Option 1 (Recommended for Vercel): Provide the entire service account JSON as a string
# FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "supto-dev", ...}'
FIREBASE_SERVICE_ACCOUNT=

# Option 2 (Local): Path to your service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./config/service-account.json

# Server Config
NODE_ENV=development
PORT=5000

# Firebase Public Config (Optional here, but useful for sync)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=
